# Backlog

Ideas, deferred work, and future features not yet turned into a spec. Append-only — items are removed automatically once `spec-flow:specify` turns them into a spec.

- [ ] B001 <one-line description> (noted YYYY-MM-DD, from <phase or context>)
